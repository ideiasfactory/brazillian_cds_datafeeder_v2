# Pyenv Virtual Environment Setup

## ✅ Configuration Complete

### Virtual Environment Details
- **Name**: `brazillian_cds_datafeeder_v2`
- **Python Version**: 3.11.0
- **Location**: `~/.pyenv/versions/brazillian_cds_datafeeder_v2/`

### Configuration Files
- `.python-version` - Auto-activates the virtualenv when entering this directory

## 🔧 Usage

### Automatic Activation
The virtual environment is automatically activated when you:
```bash
cd /home/flaviolopes/projects/personnal/trading_bots/data_feeders/brazillian_cds_datafeeder_v2
```

You should see `(brazillian_cds_datafeeder_v2)` in your prompt.

### Manual Activation (if needed)
```bash
pyenv activate brazillian_cds_datafeeder_v2
```

### Deactivation
```bash
pyenv deactivate
```

### Verify Environment
```bash
# Check Python version
python --version  # Should show: Python 3.11.0

# Check Python path
which python  # Should show: ~/.pyenv/shims/python

# List installed packages
pip list
```

## 📦 Installed Packages

```
fastapi           0.121.1
uvicorn           0.38.0
pydantic          2.12.4
starlette         0.49.3
uvloop            0.22.1
watchfiles        1.1.1
websockets        15.0.1
httptools         0.7.1
python-dotenv     1.2.1
pyyaml            6.0.3
```

## 🚀 Run the Application

```bash
# Development mode with auto-reload
uvicorn main:app --reload

# Production mode
uvicorn main:app --host 0.0.0.0 --port 8000
```

## 🔄 Managing Packages

### Install new packages
```bash
pip install <package-name>
```

### Update requirements.txt
```bash
pip freeze > requirements.txt
```

### Install from requirements.txt
```bash
pip install -r requirements.txt
```

## 🗑️ Remove Virtual Environment (if needed)

```bash
pyenv uninstall brazillian_cds_datafeeder_v2
```

## 📝 Notes

- The old `.venv` directory has been removed to avoid conflicts
- Pyenv manages the virtual environment globally
- The `.python-version` file ensures automatic activation
- VS Code should automatically detect the pyenv environment
